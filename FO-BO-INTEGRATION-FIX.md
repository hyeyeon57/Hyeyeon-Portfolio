# FO-BO 연동 수정 완료 ✅

## 해결한 문제

### 1. ✅ BO에서 추가한 프로젝트가 FO에 반영 안 되는 문제
**원인**: 
- 기존 로직이 BO 데이터와 정적 데이터를 병합하는 방식이었음
- BO에 데이터가 적으면 제대로 표시되지 않음

**해결**:
- **BO 데이터를 우선으로 사용**하도록 로직 변경
- BO에 프로젝트가 있으면 BO 데이터만 사용
- BO에 데이터가 없으면 정적 데이터 사용 (fallback)

### 2. ✅ FO에 있는 프로젝트를 BO에서도 관리
**해결**:
- `server/migrate-from-static.cjs` 스크립트 생성
- `data/projects.json`의 모든 프로젝트를 MongoDB로 마이그레이션
- 이제 BO에서 모든 프로젝트를 관리할 수 있음

## 변경된 파일

### 1. `src/components/sections/ProjectsSection.tsx`
- BO 데이터를 우선으로 사용
- 30초마다 자동 새로고침 (선택사항)
- 캐시 방지를 위한 타임스탬프 추가

### 2. `src/app/projects/page.tsx`
- BO 데이터를 우선으로 사용
- 30초마다 자동 새로고침 (선택사항)
- 캐시 방지를 위한 타임스탬프 추가

### 3. `server/migrate-from-static.cjs` (신규)
- 정적 프로젝트를 MongoDB로 마이그레이션
- 기존 프로젝트는 업데이트, 새 프로젝트는 추가

### 4. `package.json`
- `migrate:static` 스크립트 추가

## 사용 방법

### 1. 정적 프로젝트를 MongoDB로 마이그레이션
```bash
npm run migrate:static
```

이 명령을 실행하면:
- `data/projects.json`의 모든 프로젝트가 MongoDB로 이동
- 기존 프로젝트는 업데이트됨
- 새 프로젝트는 추가됨

### 2. BO에서 프로젝트 추가/수정/삭제
1. `http://localhost:3005/admin` 접속
2. 프로젝트 추가/수정/삭제
3. FO 화면 (`http://localhost:3000`) 새로고침
4. 변경사항이 자동으로 반영됨!

### 3. 자동 새로고침
- FO 화면은 30초마다 자동으로 프로젝트를 새로고침합니다
- 수동으로 새로고침하려면 `F5` 또는 `Ctrl+R` 사용

## 데이터 흐름

```
BO 서버 (MongoDB)
    ↓
Next.js API (/api/projects)
    ↓
FO 화면 (포트 3000)
```

**중요**: 이제 모든 프로젝트는 **BO에서 관리**됩니다!

## 주의사항

1. **마이그레이션은 한 번만 실행**
   - `npm run migrate:static`을 여러 번 실행해도 안전합니다
   - 기존 프로젝트는 업데이트되고, 새 프로젝트만 추가됩니다

2. **정적 데이터는 이제 fallback**
   - BO에 데이터가 없을 때만 정적 데이터 사용
   - BO에 데이터가 있으면 BO 데이터만 사용

3. **자동 새로고침**
   - 30초마다 자동으로 프로젝트를 새로고침합니다
   - 필요없으면 코드에서 `setInterval` 부분을 제거하세요

## 다음 단계

1. ✅ 마이그레이션 실행: `npm run migrate:static`
2. ✅ BO에서 프로젝트 추가 테스트
3. ✅ FO에서 프로젝트 확인
4. ✅ 프로젝트 수정/삭제 테스트

이제 BO에서 프로젝트를 추가하면 FO에 자동으로 반영됩니다! 🎉

