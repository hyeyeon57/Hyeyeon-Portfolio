# 관리자 로그인 시스템 가이드

## ✅ 로그인 시스템 구현 완료!

관리자 페이지에 로그인 기능이 추가되었습니다.

## 🔐 로그인 정보

### 기본 비밀번호
- **비밀번호**: `admin123`

### 비밀번호 변경
`.env` 파일에서 `ADMIN_PASSWORD`를 수정하세요:
```env
ADMIN_PASSWORD=원하는비밀번호
```

## 📝 사용 방법

### 1. 관리자 페이지 접속
1. FO 푸터의 "Admin Login" 클릭
2. 또는 직접 `http://localhost:3005/admin` 접속
3. **자동으로 로그인 페이지로 이동합니다**

### 2. 로그인
1. 비밀번호 입력 (`admin123`)
2. "로그인" 버튼 클릭
3. 로그인 성공 시 관리자 대시보드로 이동

### 3. 로그아웃
- 관리자 페이지 상단의 "로그아웃" 버튼 클릭
- 또는 세션 만료 시 자동으로 로그인 페이지로 이동

## 🔒 보안 기능

### 세션 기반 인증
- `express-session` 사용
- 세션 만료 시간: 24시간
- 쿠키 기반 세션 관리

### 보호된 페이지
- `/admin` - 관리자 대시보드 (로그인 필요)
- `/admin/create` - 프로젝트 생성 페이지 (로그인 필요)
- `/admin/login` - 로그인 페이지 (로그인 시 자동 리다이렉트)

### API 엔드포인트
- `POST /api/auth/login` - 로그인
- `POST /api/auth/logout` - 로그아웃
- `GET /api/auth/check` - 로그인 상태 확인

## 📁 변경된 파일

### 1. `server/index.cjs`
- `express-session` 미들웨어 추가
- 로그인/로그아웃 API 엔드포인트 추가
- `requireAuth` 미들웨어 추가
- 관리자 페이지 라우트에 인증 체크 추가

### 2. `server/admin/login.html` (신규)
- 로그인 페이지 UI
- 비밀번호 입력 폼
- 비밀번호 보기/숨기기 기능
- 로그인 API 연동

### 3. `server/admin/index.html`
- 로그아웃 버튼 추가
- 로그인 상태 확인 기능 추가

### 4. `server/admin/create.html`
- 로그아웃 버튼 추가
- 로그인 상태 확인 기능 추가

### 5. `package.json`
- `express-session` 의존성 추가

### 6. `.env`
- `ADMIN_PASSWORD` 환경 변수 추가
- `SESSION_SECRET` 환경 변수 추가

## ⚙️ 환경 변수 설정

`.env` 파일에 다음을 추가하세요:
```env
ADMIN_PASSWORD=원하는비밀번호
SESSION_SECRET=랜덤시크릿키
```

## 🚀 작동 흐름

1. **사용자가 `/admin` 접속**
   - 로그인 안 되어 있으면 → `/admin/login`으로 리다이렉트
   - 로그인 되어 있으면 → 관리자 대시보드 표시

2. **로그인 페이지에서 비밀번호 입력**
   - 비밀번호 확인
   - 성공 시 세션 생성
   - 관리자 대시보드로 이동

3. **관리자 페이지 사용 중**
   - 페이지 로드 시 로그인 상태 확인
   - 로그인 안 되어 있으면 자동으로 로그인 페이지로 이동

4. **로그아웃**
   - 세션 삭제
   - 로그인 페이지로 이동

## 💡 보안 권장사항

1. **프로덕션 환경에서는**:
   - `.env` 파일에 강력한 비밀번호 설정
   - `SESSION_SECRET`을 랜덤한 긴 문자열로 설정
   - HTTPS 사용 시 `secure: true` 설정

2. **비밀번호 관리**:
   - 기본 비밀번호(`admin123`)를 반드시 변경하세요
   - 복잡한 비밀번호 사용 권장

이제 관리자 페이지에 안전하게 로그인할 수 있습니다! 🔐

