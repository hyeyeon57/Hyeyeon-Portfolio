# 서버 재시작 가이드

## 에러 해결 완료 사항
✅ 모든 Node 프로세스 종료 완료
✅ `.next` 폴더 삭제 완료

## 서버 재시작 순서

### 1. 백엔드 서버 시작 (터미널 1)
```bash
cd Hyeyeon-Portfolio
npm run dev:server
```

백엔드 서버가 정상적으로 시작되면:
- `MongoDB 연결 성공` 메시지 확인
- `서버가 포트 3005에서 실행 중입니다` 메시지 확인

### 2. 프론트엔드 서버 시작 (터미널 2)
```bash
cd Hyeyeon-Portfolio
npm run dev
```

프론트엔드 서버가 정상적으로 시작되면:
- `Ready in X seconds` 메시지 확인
- `http://localhost:3000` 주소 확인

## 문제 해결 체크리스트

### 백엔드 서버가 시작되지 않는 경우:
1. MongoDB가 실행 중인지 확인
2. `.env` 파일에 `MONGODB_URI`가 올바르게 설정되어 있는지 확인
3. 포트 3005가 다른 프로그램에 의해 사용 중인지 확인

### 프론트엔드 서버가 시작되지 않는 경우:
1. `.next` 폴더가 완전히 삭제되었는지 확인
2. `node_modules` 폴더가 손상되지 않았는지 확인
3. 포트 3000이 다른 프로그램에 의해 사용 중인지 확인

### 500 에러가 계속 발생하는 경우:
1. 브라우저 캐시 삭제 (Ctrl + Shift + Delete)
2. 시크릿 모드에서 테스트
3. 백엔드 서버 로그 확인

## 주의사항
- **반드시 백엔드 서버를 먼저 시작**한 후 프론트엔드 서버를 시작하세요
- 두 서버가 모두 실행 중이어야 정상 작동합니다
- 에러가 발생하면 두 서버를 모두 종료하고 다시 시작하세요

